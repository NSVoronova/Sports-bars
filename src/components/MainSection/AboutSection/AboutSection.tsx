import MainSectionTitle from '../MainSectionTitle/MainSectionTitle';
import AboutContent from './AboutContent/AboutContent';

const AboutSection = () => {
  return (
    <section>
      <MainSectionTitle title='Quiénes somos' />
      <AboutContent />
    </section>
  );
};

export default AboutSection;
